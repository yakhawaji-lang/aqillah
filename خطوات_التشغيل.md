# 📋 خطوات تشغيل نظام عَقِلْها (Aqillah)

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

1. **Node.js** (الإصدار 18 أو أحدث)
   - تحميل من: https://nodejs.org/
   - التحقق من التثبيت: `node --version`

2. **XAMPP** (لإدارة MySQL)
   - تحميل من: https://www.apachefriends.org/
   - تأكد من تثبيت MySQL

3. **Git** (اختياري)
   - تحميل من: https://git-scm.com/

---

## 🚀 خطوات التشغيل خطوة بخطوة

### الخطوة 1: فتح XAMPP وتشغيل MySQL

1. افتح **XAMPP Control Panel**
2. اضغط على زر **Start** بجانب **MySQL**
3. تأكد من أن MySQL يعمل (سيظهر باللون الأخضر)

> ⚠️ **مهم**: يجب أن يكون MySQL يعمل قبل المتابعة

---

### الخطوة 2: فتح مجلد المشروع

افتح **PowerShell** أو **Command Prompt** وانتقل إلى مجلد المشروع:

```powershell
cd C:\Aqqilha
```

> 💡 إذا كان المشروع في مكان آخر، استبدل المسار بمسار المشروع الخاص بك

---

### الخطوة 3: تثبيت المكتبات المطلوبة

قم بتثبيت جميع المكتبات المطلوبة:

```powershell
npm install
```

> ⏱️ قد يستغرق هذا الأمر بضع دقائق حسب سرعة الإنترنت

---

### الخطوة 4: إعداد قاعدة البيانات

#### أ) إنشاء قاعدة بيانات MySQL

1. افتح **phpMyAdmin** من XAMPP:
   - افتح المتصفح واذهب إلى: `http://localhost/phpmyadmin`
   - أو اضغط على **Admin** بجانب MySQL في XAMPP

2. أنشئ قاعدة بيانات جديدة:
   - اضغط على **New** في القائمة الجانبية
   - أدخل اسم قاعدة البيانات: `aqillah`
   - اختر **Collation**: `utf8mb4_unicode_ci`
   - اضغط **Create**

#### ب) إعداد ملف البيئة (.env)

1. في مجلد المشروع (`C:\Aqqilha`)، أنشئ ملف جديد باسم `.env`

2. افتح الملف وأضف المحتوى التالي:

```env
DATABASE_URL="mysql://root:@localhost:3306/aqillah"
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

> 📝 **ملاحظة**: 
> - إذا كان لديك كلمة مرور لـ MySQL، أضفها بعد `root:` مثل: `mysql://root:password@localhost:3306/aqillah`
> - إذا كان MySQL يعمل على منفذ آخر، غيّر `3306` إلى المنفذ الصحيح

---

### الخطوة 5: إنشاء جداول قاعدة البيانات

قم بتنفيذ الأوامر التالية بالترتيب:

```powershell
# 1. توليد Prisma Client
npm run db:generate

# 2. إنشاء الجداول في قاعدة البيانات
npm run db:push
```

> ✅ إذا نجحت العملية، ستظهر رسالة "Your database is now in sync with your Prisma schema"

---

### الخطوة 6: إضافة بيانات تجريبية (اختياري)

لإضافة بيانات تجريبية للاختبار:

```powershell
npm run db:seed
```

> 💡 هذا الأمر يضيف بيانات تجريبية مثل:
> - مقاطع طرق
> - بيانات ازدحام
> - تنبيهات
> - تنبؤات

---

### الخطوة 7: تشغيل المشروع

قم بتشغيل المشروع في وضع التطوير:

```powershell
npm run dev
```

> ⏱️ انتظر حتى تظهر رسالة: `Ready - started server on 0.0.0.0:3000`

---

### الخطوة 8: فتح التطبيق

افتح المتصفح واذهب إلى:

```
http://localhost:3000
```

---

## 📱 الصفحات المتاحة

بعد تشغيل المشروع، يمكنك الوصول إلى:

1. **الصفحة الرئيسية**: `http://localhost:3000`
2. **تطبيق المستخدم**: `http://localhost:3000/user`
3. **لوحة التحكم الحكومية**: `http://localhost:3000/government`
4. **لوحة التحكم العامة**: `http://localhost:3000/dashboard`

---

## 🔄 إضافة بيانات حية (اختياري)

لإضافة بيانات حية لمدة نصف ساعة:

```powershell
npm run db:live
```

> 💡 اترك هذا الأمر يعمل في نافذة منفصلة لإضافة بيانات مستمرة

---

## 📊 فتح Prisma Studio (لإدارة قاعدة البيانات)

لعرض وإدارة البيانات مباشرة:

```powershell
npm run db:studio
```

ثم افتح المتصفح على: `http://localhost:5555`

---

## 🛠️ أوامر مفيدة أخرى

### بناء المشروع للإنتاج

```powershell
npm run build
npm start
```

### فحص الأخطاء البرمجية

```powershell
npm run lint
```

### إضافة بيانات تاريخية (سنة كاملة)

```powershell
npm run db:historical
```

> ⚠️ **تحذير**: هذا الأمر قد يستغرق وقتًا طويلاً

---

## ❌ حل المشاكل الشائعة

### المشكلة 1: خطأ "Can't reach database server"

**الحل**:
- تأكد من أن MySQL يعمل في XAMPP
- تحقق من أن المنفذ 3306 متاح
- تأكد من صحة `DATABASE_URL` في ملف `.env`

### المشكلة 2: خطأ "prisma is not recognized"

**الحل**:
```powershell
npm install
npm run db:generate
```

### المشكلة 3: خطأ "Port 3000 is already in use"

**الحل**:
- أغلق أي تطبيق يستخدم المنفذ 3000
- أو غيّر المنفذ في `package.json`:
  ```json
  "dev": "next dev -p 3001"
  ```

### المشكلة 4: لا تظهر البيانات في الصفحات

**الحل**:
```powershell
# أضف بيانات تجريبية
npm run db:seed

# أو أضف بيانات حية
npm run db:live
```

### المشكلة 5: خطأ في بناء المشروع

**الحل**:
```powershell
# احذف مجلد .next وأعد البناء
rm -r .next
npm run build
```

---

## 📝 ملاحظات مهمة

1. **يجب أن يكون MySQL يعمل** قبل تشغيل المشروع
2. **لا تغلق نافذة PowerShell** التي يعمل فيها `npm run dev`
3. **احفظ التغييرات** في الملفات لتحديث الصفحة تلقائيًا (Hot Reload)
4. **استخدم Ctrl+C** لإيقاف الخادم

---

## 🎯 الخطوات السريعة (ملخص)

```powershell
# 1. تشغيل MySQL في XAMPP

# 2. الانتقال لمجلد المشروع
cd C:\Aqqilha

# 3. تثبيت المكتبات
npm install

# 4. إنشاء قاعدة البيانات في phpMyAdmin (اسم: aqillah)

# 5. إنشاء ملف .env وإضافة DATABASE_URL

# 6. إنشاء الجداول
npm run db:generate
npm run db:push

# 7. إضافة بيانات تجريبية (اختياري)
npm run db:seed

# 8. تشغيل المشروع
npm run dev

# 9. فتح المتصفح على http://localhost:3000
```

---

## 📞 الدعم

إذا واجهت أي مشكلة:

1. تحقق من أن جميع المتطلبات مثبتة
2. تأكد من أن MySQL يعمل
3. راجع قسم "حل المشاكل الشائعة" أعلاه
4. تحقق من ملفات السجلات (logs) للأخطاء

---

**تم التحديث**: 2024
**الإصدار**: 1.0.0

