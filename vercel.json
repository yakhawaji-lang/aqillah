{
  "buildCommand": "DATABASE_URL=\"mysql://user:pass@localhost:3306/db\" prisma generate && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --include=dev",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NODE_ENV": "production"
  }
}

