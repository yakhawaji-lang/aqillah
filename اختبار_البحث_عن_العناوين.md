# اختبار البحث عن العناوين

## خطوات الاختبار

### 1. التحقق من المفتاح
افتح في المتصفح:
```
http://localhost:3000/api/test-places
```

يجب أن ترى:
```json
{
  "success": true,
  "config": {
    "placesApiKey": "AIzaSyB4R...",
    "placesApiKeyExists": true
  }
}
```

### 2. اختبار API مباشرة
افتح في المتصفح:
```
http://localhost:3000/api/places/autocomplete?input=مطار
```

يجب أن ترى:
- إما نتائج JSON
- أو رسالة خطأ واضحة

### 3. فتح Console في المتصفح
1. اضغط `F12`
2. اذهب إلى تبويب **Console**
3. ابحث عن:
   - `🔍 Fetching autocomplete for:` - يعني أن الطلب بدأ
   - `✅ Autocomplete response:` - يعني أن النتائج وصلت
   - `❌ Error fetching autocomplete:` - يعني أن هناك خطأ

### 4. اختبار في الصفحة
1. اذهب إلى `/user`
2. اضغط على تبويب **المسار**
3. اكتب في حقل **الوجهة**: "مطار الملك خالد"
4. يجب أن تظهر النتائج بعد ثانية أو ثانيتين

## الأخطاء الشائعة

### خطأ 500 Internal Server Error
**السبب**: Places API غير مفعل أو المفتاح غير صحيح

**الحل**:
1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. فعل **Places API**
3. تحقق من المفتاح `AQILLAH_PLACES_KEY`

### لا توجد نتائج
**السبب**: قد يكون البحث صحيح لكن لا توجد نتائج

**الحل**: جرب بحث آخر مثل:
- "مطار"
- "جامعة"
- "مستشفى"
- "مركز"

### خطأ REQUEST_DENIED
**السبب**: المفتاح غير مصرح به لهذا الموقع

**الحل**:
1. اذهب إلى **Credentials** في Google Cloud Console
2. اضغط على المفتاح
3. أضف `localhost:3000/*` في **Application restrictions**

## التحقق من السيرفر Logs

في terminal الذي يعمل فيه `npm run dev`، ابحث عن:
- `🔍 Autocomplete API called:` - يعني أن الطلب وصل للـ API
- `📞 Calling googleMapsService.autocomplete` - يعني أن الدالة بدأت
- `✅ Autocomplete results:` - يعني أن النتائج جاهزة
- `❌ Error` - يعني أن هناك خطأ

## ملاحظات

- البحث يحتاج إلى حرفين على الأقل
- النتائج قد تأخذ ثانية أو ثانيتين
- تأكد من أن Places API مفعل في Google Cloud Console

