# دليل رفع المشروع على Vercel - خطوة بخطوة 🚀

## 📋 المتطلبات الأساسية

- ✅ حساب GitHub (مجاني)
- ✅ حساب Vercel (مجاني)
- ✅ Node.js مثبت (للتجربة المحلية فقط)

---

## 🎯 الخطوات الكاملة

### الخطوة 1: إعداد Git في المشروع

#### 1.1 فتح Terminal في مجلد المشروع

```powershell
# تأكد أنك في مجلد المشروع
cd C:\Aqqilha
```

#### 1.2 تهيئة Git

```powershell
git init
```

#### 1.3 إضافة جميع الملفات

```powershell
git add .
```

#### 1.4 عمل Commit أولي

```powershell
git commit -m "رفع المشروع الأولي"
```

---

### الخطوة 2: رفع المشروع على GitHub

#### 2.1 إنشاء مستودع جديد على GitHub

1. اذهب إلى https://github.com
2. سجل دخول (أو أنشئ حساب جديد)
3. اضغط على زر **"+"** في الأعلى → **"New repository"**
4. املأ البيانات:
   - **Repository name:** `aqillah` (أو أي اسم تريده)
   - **Description:** `نظام وطني ذكي لتحليل الازدحام المروري`
   - **Public** أو **Private** (اختر ما تريد)
   - ❌ **لا** تضع علامة على "Initialize with README"
5. اضغط **"Create repository"**

#### 2.2 ربط المشروع المحلي بـ GitHub

بعد إنشاء المستودع، GitHub سيعرض لك الأوامر. استخدم هذه:

```powershell
# استبدل YOUR_USERNAME باسمك على GitHub
git remote add origin https://github.com/YOUR_USERNAME/aqillah.git
git branch -M main
git push -u origin main
```

**مثال:**
```powershell
git remote add origin https://github.com/ahmed123/aqillah.git
git branch -M main
git push -u origin main
```

**إذا طُلب منك اسم المستخدم وكلمة المرور:**
- اسم المستخدم: اسمك على GitHub
- كلمة المرور: استخدم **Personal Access Token** (انظر الخطوة 2.3)

#### 2.3 إنشاء Personal Access Token (إذا طُلب)

1. اذهب إلى GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. اضغط **"Generate new token (classic)"**
3. املأ البيانات:
   - **Note:** `Vercel Deployment`
   - **Expiration:** اختر المدة (90 يوم أو سنة)
   - ✅ **repo** (Full control of private repositories)
4. اضغط **"Generate token"**
5. انسخ الرمز (سيظهر مرة واحدة فقط!)
6. استخدمه ككلمة مرور عند `git push`

---

### الخطوة 3: إنشاء حساب على Vercel

#### 3.1 التسجيل في Vercel

1. اذهب إلى https://vercel.com
2. اضغط **"Sign Up"**
3. اختر **"Continue with GitHub"**
4. سجل دخول بحساب GitHub
5. وافق على الصلاحيات

**✅ الآن لديك حساب Vercel مجاني!**

---

### الخطوة 4: رفع المشروع على Vercel

#### 4.1 إنشاء مشروع جديد

1. في Vercel Dashboard، اضغط **"Add New..."** → **"Project"**
2. ستظهر قائمة المستودعات من GitHub
3. ابحث عن `aqillah` (أو اسم المستودع الذي أنشأته)
4. اضغط **"Import"** بجانب المستودع

#### 4.2 إعداد المشروع

Vercel سيكتشف تلقائياً أن هذا مشروع Next.js! ✅

**الإعدادات:**
- **Framework Preset:** Next.js (سيتم اختياره تلقائياً)
- **Root Directory:** `./` (اتركه كما هو)
- **Build Command:** `npm run build` (سيتم اختياره تلقائياً)
- **Output Directory:** `.next` (سيتم اختياره تلقائياً)
- **Install Command:** `npm install` (سيتم اختياره تلقائياً)

**لا تغير أي شيء، فقط اضغط "Deploy"!**

---

### الخطوة 5: إضافة Environment Variables

**⚠️ مهم جداً:** قبل الضغط على "Deploy"، أضف متغيرات البيئة!

#### 5.1 إضافة متغيرات البيئة في Vercel

في صفحة إعداد المشروع، ابحث عن قسم **"Environment Variables"**

#### 5.2 المتغيرات المطلوبة

أضف هذه المتغيرات واحدة تلو الأخرى:

**1. قاعدة البيانات (DATABASE_URL):**
```
Key: DATABASE_URL
Value: mysql://username:password@host:3306/database_name
```

**⚠️ إذا لم تكن لديك قاعدة بيانات بعد:**
- يمكنك استخدام PlanetScale (مجاني) - انظر الخطوة 6
- أو اتركه فارغاً الآن وأضفه لاحقاً

**2. Google Maps API Keys:**
```
Key: NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY
Value: your_google_maps_api_key

Key: AQILLAH_MAPS_WEB_KEY
Value: your_google_maps_api_key

Key: AQILLAH_ROUTES_KEY
Value: your_routes_api_key

Key: AQILLAH_PLACES_KEY
Value: your_places_api_key
```

**3. OpenWeatherMap API:**
```
Key: OPENWEATHER_API_KEY
Value: your_openweather_api_key
```

**4. إعدادات التطبيق:**
```
Key: NEXT_PUBLIC_APP_URL
Value: https://your-project.vercel.app
(سيتم تحديثه تلقائياً بعد الرفع)

Key: NODE_ENV
Value: production
```

#### 5.3 كيفية إضافة متغير

1. اضغط **"Add"** بجانب Environment Variables
2. اكتب **Key** (مثل `DATABASE_URL`)
3. اكتب **Value** (مثل `mysql://...`)
4. اختر **Environment:** Production, Preview, Development (أو اختر Production فقط)
5. اضغط **"Save"**
6. كرر لكل متغير

---

### الخطوة 6: Deploy (الرفع)

#### 6.1 بدء الرفع

1. بعد إضافة جميع Environment Variables
2. اضغط **"Deploy"** في الأسفل
3. انتظر حتى يكتمل البناء (عادة 2-5 دقائق)

#### 6.2 مراقبة عملية البناء

سترى:
- ✅ Installing dependencies
- ✅ Building project
- ✅ Deploying

**إذا حدث خطأ:**
- تحقق من Logs في Vercel
- تأكد من أن جميع Environment Variables صحيحة
- تأكد من أن المشروع يبني محلياً (`npm run build`)

#### 6.3 النتيجة

بعد اكتمال البناء:
- ✅ الموقع متاح على: `https://your-project.vercel.app`
- ✅ يمكنك فتح الرابط في المتصفح
- ✅ الموقع يعمل!

---

### الخطوة 7: إعداد قاعدة البيانات (PlanetScale)

#### 7.1 إنشاء حساب PlanetScale

1. اذهب إلى https://planetscale.com
2. اضغط **"Sign Up"**
3. اختر **"Continue with GitHub"**
4. سجل دخول بحساب GitHub

#### 7.2 إنشاء قاعدة بيانات

1. في PlanetScale Dashboard، اضغط **"Create database"**
2. املأ البيانات:
   - **Database name:** `aqillah`
   - **Region:** اختر الأقرب (مثل `us-east-1`)
   - **Plan:** اختر **"Free"**
3. اضغط **"Create database"**

#### 7.3 الحصول على DATABASE_URL

1. بعد إنشاء قاعدة البيانات، اضغط على اسمها
2. اذهب إلى **"Settings"** → **"Passwords"**
3. اضغط **"Create password"**
4. اختر اسم للـ password (مثل `production`)
5. اضغط **"Create password"**
6. انسخ **"Connection string"** (سيبدو مثل: `mysql://...`)

#### 7.4 إضافة DATABASE_URL إلى Vercel

1. اذهب إلى Vercel Dashboard
2. اختر مشروعك
3. اذهب إلى **"Settings"** → **"Environment Variables"**
4. أضف:
   ```
   Key: DATABASE_URL
   Value: mysql://... (الذي نسخته من PlanetScale)
   ```
5. اضغط **"Save"**
6. اضغط **"Redeploy"** لتطبيق التغييرات

#### 7.5 إعداد Schema قاعدة البيانات

**الطريقة 1: من PlanetScale Dashboard**

1. في PlanetScale Dashboard، اختر قاعدة البيانات
2. اذهب إلى **"Branches"** → **"main"**
3. اضغط **"Schema"**
4. اضغط **"Push schema"**
5. انسخ محتوى ملف `prisma/schema.prisma` والصقه
6. اضغط **"Apply"**

**الطريقة 2: من Terminal (محلي)**

```powershell
# تأكد من تحديث .env بالـ DATABASE_URL الجديد
npx prisma db push
npx prisma generate
```

---

### الخطوة 8: تحديث Google Cloud Console

#### 8.1 إضافة Domain إلى API Restrictions

1. اذهب إلى https://console.cloud.google.com
2. اختر مشروعك
3. اذهب إلى **"APIs & Services"** → **"Credentials"**
4. اضغط على API Key الذي تستخدمه
5. في **"Application restrictions"**:
   - اختر **"HTTP referrers (web sites)"**
   - اضغط **"Add an item"**
   - أضف:
     ```
     https://your-project.vercel.app/*
     *.vercel.app/*
     ```
6. اضغط **"Save"**

---

### الخطوة 9: اختبار الموقع

#### 9.1 فتح الموقع

افتح الرابط في المتصفح:
```
https://your-project.vercel.app
```

#### 9.2 اختبار الصفحات

- ✅ `/` - الصفحة الرئيسية
- ✅ `/map` - الخريطة
- ✅ `/user` - واجهة المستخدم
- ✅ `/government` - لوحة التحكم الحكومية

#### 9.3 اختبار APIs

افتح في المتصفح:
```
https://your-project.vercel.app/api/traffic
https://your-project.vercel.app/api/alerts
```

---

## 🔄 التحديثات المستقبلية

### عند تعديل أي ملف:

```powershell
# في مجلد المشروع
git add .
git commit -m "وصف التغييرات"
git push origin main
```

**Vercel سيتحدث تلقائياً!** 🎉

---

## ⚠️ حل المشاكل الشائعة

### مشكلة: خطأ في البناء

**الحل:**
1. تحقق من Logs في Vercel
2. جرب البناء محلياً:
   ```powershell
   npm run build
   ```
3. تأكد من أن جميع Environment Variables صحيحة

### مشكلة: Google Maps لا يعمل

**الحل:**
1. تأكد من إضافة Domain إلى Google Cloud Console
2. تحقق من API Key في Environment Variables
3. تأكد من تفعيل APIs المطلوبة:
   - Maps JavaScript API
   - Directions API
   - Geocoding API
   - Places API

### مشكلة: قاعدة البيانات لا تعمل

**الحل:**
1. تحقق من `DATABASE_URL` في Environment Variables
2. تأكد من إعداد Schema في PlanetScale
3. جرب الاتصال محلياً:
   ```powershell
   npx prisma studio
   ```

### مشكلة: الموقع لا يفتح

**الحل:**
1. تحقق من أن الرفع اكتمل بنجاح
2. انتظر دقيقة أو دقيقتين (أحياناً يحتاج وقت)
3. جرب فتح الرابط في نافذة خاصة (Incognito)

---

## ✅ قائمة التحقق

قبل الرفع:
- [ ] تم إعداد Git في المشروع
- [ ] تم رفع المشروع على GitHub
- [ ] تم إنشاء حساب Vercel
- [ ] تم إضافة جميع Environment Variables
- [ ] تم إنشاء قاعدة بيانات (PlanetScale)
- [ ] تم تحديث Google Cloud Console

بعد الرفع:
- [ ] الموقع يفتح في المتصفح
- [ ] جميع الصفحات تعمل
- [ ] Google Maps يعمل
- [ ] APIs تعمل
- [ ] قاعدة البيانات متصلة

---

## 🎉 مبروك!

الآن موقعك متاح على:
```
https://your-project.vercel.app
```

**يمكن الوصول من:**
- ✅ أي متصفح
- ✅ أي موبايل
- ✅ أي تطبيق
- ✅ أي مكان في العالم!

---

## 📝 ملاحظات مهمة

1. **كل تحديث على GitHub يحدث Vercel تلقائياً**
2. **Vercel مجاني تماماً للاستخدام الشخصي**
3. **PlanetScale مجاني تماماً (5GB storage)**
4. **SSL مجاني تلقائي**
5. **Domain مجاني: `*.vercel.app`**

---

**بالتوفيق! 🚀**

