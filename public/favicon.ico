# حل مشكلة تحميل ملفات Next.js

## المشكلة
الأخطاء تشير إلى أن ملفات Next.js لا تُحمل:
- `GET http://localhost:3000/_next/static/chunks/main-app.js 404`
- `GET http://localhost:3000/_next/static/chunks/app/error.js 404`

## الحل

### 1. مسح Cache وإعادة التشغيل

```bash
# أوقف dev server (Ctrl+C)
# ثم شغّل:
Remove-Item -Path .next -Recurse -Force
npm run dev
```

### 2. تحقق من أن dev server يعمل

افتح:
```
http://localhost:3000/api/test-maps
```

يجب أن ترى JSON response.

### 3. افتح صفحة الاختبار

```
http://localhost:3000/test-map
```

### 4. إذا استمرت المشكلة

1. **أغلق جميع نوافذ المتصفح**
2. **أوقف dev server** (Ctrl+C في Terminal)
3. **امسح cache المتصفح**:
   - Chrome: Ctrl+Shift+Delete → Clear cache
   - أو افتح في Incognito Mode
4. **أعد تشغيل dev server**:
   ```bash
   npm run dev
   ```
5. **افتح المتصفح من جديد**

---

## ملاحظات

- الأخطاء المتعلقة بـ `chrome-extension` و `favicon.ico` **ليست مشكلة** - يمكن تجاهلها
- المشكلة الحقيقية هي في تحميل ملفات Next.js
- بعد مسح `.next` وإعادة التشغيل، يجب أن تعمل الصفحات

---

## بعد الإصلاح

افتح:
- `http://localhost:3000/test-map` - صفحة الاختبار
- `http://localhost:3000/user` - صفحة المستخدم

يجب أن ترى الخريطة! 🎉

