# رفع سريع على الاستضافة - خطوات مختصرة

## 🚀 الطريقة الأسهل: Vercel + PlanetScale

### الخطوة 1: إعداد PlanetScale (قاعدة البيانات)

1. اذهب إلى https://planetscale.com
2. سجل حساب جديد
3. أنشئ Database جديد باسم `aqillah`
4. انسخ `DATABASE_URL` من إعدادات Database

### الخطوة 2: إعداد Vercel

1. اذهب إلى https://vercel.com
2. سجل بحساب GitHub
3. اضغط "New Project"
4. اختر المستودع من GitHub
5. Vercel سيكتشف Next.js تلقائياً

### الخطوة 3: إضافة متغيرات البيئة في Vercel

في إعدادات المشروع → Environment Variables، أضف:

```
DATABASE_URL=mysql://username:password@host:3306/aqillah
NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY=your_key
AQILLAH_MAPS_WEB_KEY=your_key
AQILLAH_ROUTES_KEY=your_key
AQILLAH_PLACES_KEY=your_key
OPENWEATHER_API_KEY=your_key
NEXT_PUBLIC_APP_URL=https://your-project.vercel.app
NODE_ENV=production
```

### الخطوة 4: Deploy

1. اضغط "Deploy"
2. انتظر حتى يكتمل البناء
3. الموقع سيكون متاحاً على `your-project.vercel.app`

### الخطوة 5: إعداد قاعدة البيانات

بعد الرفع، في Terminal على Vercel:

```bash
npx prisma db push
npm run db:generate
```

---

## 📋 قائمة المتغيرات المطلوبة

**قاعدة البيانات:**
- `DATABASE_URL`

**Google Maps:**
- `NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY`
- `AQILLAH_MAPS_WEB_KEY`
- `AQILLAH_ROUTES_KEY`
- `AQILLAH_PLACES_KEY`

**OpenWeatherMap:**
- `OPENWEATHER_API_KEY`

**إعدادات:**
- `NEXT_PUBLIC_APP_URL`
- `NODE_ENV=production`

---

## ⚠️ مهم جداً

1. **تحديث Google Cloud Console:**
   - أضف Domain الاستضافة إلى API Key restrictions
   - مثال: `your-project.vercel.app`

2. **لا ترفع ملف `.env`:**
   - تأكد من وجوده في `.gitignore`

3. **اختبار محلي أولاً:**
   ```bash
   npm run build
   npm start
   ```

---

**للتفاصيل الكاملة:** راجع `دليل_رفع_الموقع_على_الاستضافة.md`

