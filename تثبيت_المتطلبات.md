# 📦 تثبيت متطلبات Android - عَقِلْها

## الطريقة السريعة (موصى به)

### استخدام السكريبت التلقائي

```powershell
npm run android:install
```

أو مباشرة:

```powershell
.\scripts\install-requirements.ps1
```

---

## فحص سريع

للتحقق السريع من المتطلبات:

```powershell
npm run android:check
```

---

## التثبيت اليدوي

### 1. Node.js (18+)

1. **التحميل**: https://nodejs.org/
2. اختر **LTS** (Long Term Support)
3. ثبّت مع الخيارات الافتراضية
4. **التحقق**:
   ```powershell
   node --version
   npm --version
   ```

---

### 2. Java JDK (17+)

#### الخيار 1: Oracle JDK
1. **التحميل**: https://www.oracle.com/java/technologies/downloads/
2. اختر **Windows x64 Installer**
3. ثبّت مع الخيارات الافتراضية

#### الخيار 2: Eclipse Temurin (موصى به)
1. **التحميل**: https://adoptium.net/
2. اختر **JDK 17** → **Windows x64**
3. ثبّت مع الخيارات الافتراضية

#### إعداد JAVA_HOME

1. افتح **System Properties**:
   - اضغط `Win + R`
   - اكتب `sysdm.cpl`
   - اضغط Enter

2. **Environment Variables**:
   - اضغط **Environment Variables**
   - في **System variables**، اضغط **New**
   - **Variable name**: `JAVA_HOME`
   - **Variable value**: `C:\Program Files\Java\jdk-17` (أو مسار التثبيت الفعلي)
   - اضغط **OK**

3. أضف إلى **Path**:
   - اختر **Path** → **Edit**
   - اضغط **New**
   - أضف: `%JAVA_HOME%\bin`
   - اضغط **OK**

4. **التحقق**:
   ```powershell
   java -version
   javac -version
   echo $env:JAVA_HOME
   ```

---

### 3. Android Studio

1. **التحميل**: https://developer.android.com/studio
2. ثبّت مع الخيارات الافتراضية
3. عند الفتح لأول مرة:
   - اختر **Standard** installation
   - انتظر تحميل Android SDK
   - قد يستغرق 10-20 دقيقة

#### إعداد ANDROID_HOME

1. افتح **System Properties** → **Environment Variables**

2. أضف متغير جديد:
   - **Variable name**: `ANDROID_HOME`
   - **Variable value**: `C:\Users\YourUsername\AppData\Local\Android\Sdk`
   - (استبدل YourUsername باسم المستخدم الخاص بك)

3. أضف إلى **Path**:
   - `%ANDROID_HOME%\platform-tools`
   - `%ANDROID_HOME%\tools`
   - `%ANDROID_HOME%\tools\bin`

4. **التحقق**:
   ```powershell
   adb version
   echo $env:ANDROID_HOME
   ```

---

### 4. Android SDK Components

من Android Studio:

1. افتح **Tools** → **SDK Manager**
2. تأكد من تثبيت:
   - ✅ **Android SDK Platform-Tools**
   - ✅ **Android SDK Build-Tools** (أحدث إصدار)
   - ✅ **Android SDK Platform** (API 33 أو أحدث)
   - ✅ **Google Play services**
   - ✅ **Android SDK Command-line Tools**

---

## التحقق من التثبيت الكامل

بعد تثبيت كل شيء، شغّل:

```powershell
npm run android:check
```

أو للفحص التفصيلي:

```powershell
npm run android:install
```

---

## حل المشاكل

### المشكلة: "java is not recognized"

**الحل**:
1. تأكد من إضافة `%JAVA_HOME%\bin` إلى Path
2. أعد تشغيل PowerShell
3. تحقق: `java -version`

### المشكلة: "adb is not recognized"

**الحل**:
1. تأكد من إضافة `%ANDROID_HOME%\platform-tools` إلى Path
2. أعد تشغيل PowerShell
3. تحقق: `adb version`

### المشكلة: "JAVA_HOME is not set"

**الحل**:
1. افتح Environment Variables
2. أضف `JAVA_HOME` = مسار JDK
3. أعد تشغيل PowerShell

### المشكلة: "ANDROID_HOME is not set"

**الحل**:
1. افتح Environment Variables
2. أضف `ANDROID_HOME` = `C:\Users\YourUsername\AppData\Local\Android\Sdk`
3. أعد تشغيل PowerShell

---

## الخطوات التالية

بعد تثبيت جميع المتطلبات:

```powershell
# 1. فحص المتطلبات
npm run android:check

# 2. إعداد Android
npm run android:setup

# 3. بناء المشروع
npm run android:build
```

---

## روابط التحميل السريعة

- **Node.js**: https://nodejs.org/
- **Java JDK**: https://adoptium.net/ (موصى به)
- **Android Studio**: https://developer.android.com/studio

---

**تم التحديث**: 2024

