# دليل البدء السريع - عَقِلْها

## البدء السريع في 5 دقائق

### الخطوة 1: تثبيت الحزم

```bash
npm install
```

### الخطوة 2: إعداد قاعدة البيانات

#### خيار أ: استخدام Docker (موصى به)

```bash
# تشغيل PostgreSQL
docker-compose up -d

# استخدام هذه الـ DATABASE_URL في ملف .env
DATABASE_URL="postgresql://aqillah:aqillah_password@localhost:5432/aqillah?schema=public"
```

#### خيار ب: استخدام PostgreSQL محلي

1. قم بتثبيت PostgreSQL على جهازك
2. أنشئ قاعدة بيانات جديدة:
```sql
CREATE DATABASE aqillah;
```

3. أضف الـ DATABASE_URL في ملف `.env`:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/aqillah?schema=public"
```

### الخطوة 3: إعداد قاعدة البيانات

```bash
# توليد Prisma Client
npm run db:generate

# إنشاء الجداول
npm run db:push

# إضافة بيانات تجريبية (اختياري)
npm run db:seed
```

### الخطوة 4: تشغيل المشروع

```bash
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

## الصفحات المتاحة

- `/dashboard` - لوحة التحكم الرئيسية
- `/map` - خريطة تفاعلية
- `/alerts` - التنبيهات
- `/government` - لوحة التحكم الحكومية

## إضافة بيانات تجريبية

### عبر API

```bash
curl -X POST http://localhost:3000/api/seed
```

### عبر Prisma Studio

```bash
npm run db:studio
```

## استكشاف الأخطاء

### مشكلة: لا يمكن الاتصال بقاعدة البيانات

- تأكد من تشغيل PostgreSQL
- تحقق من صحة `DATABASE_URL` في ملف `.env`
- تأكد من أن قاعدة البيانات موجودة

### مشكلة: أخطاء في Prisma

```bash
# إعادة توليد Prisma Client
npm run db:generate

# إعادة مزامنة قاعدة البيانات
npm run db:push
```

### مشكلة: الخريطة لا تظهر

- تأكد من اتصال الإنترنت (Leaflet يحتاج لتحميل الخرائط)
- تحقق من console المتصفح للأخطاء

## الخطوات التالية

1. ✅ اقرأ [README.md](./README.md) للتفاصيل الكاملة
2. ✅ راجع [ARCHITECTURE.md](./docs/ARCHITECTURE.md) لفهم البنية
3. ✅ ابدأ بالتطوير!

## الدعم

للمساعدة، راجع:
- [README.md](./README.md)
- [ARCHITECTURE.md](./docs/ARCHITECTURE.md)

---

**عَقِلْها** - نظام وطني ذكي 🚗📊

