# خطوات إنشاء مستودع GitHub وربطه بالمشروع 🚀

## 📋 الخطوات السريعة

### الخطوة 1: إنشاء مستودع جديد على GitHub

من الصورة التي أرسلتها، يمكنك:

1. **اضغط على زر "New"** (الأخضر بجانب "Top repositories")
   - أو اضغط على زر "+" في الأعلى → "New repository"

2. **املأ البيانات:**
   - **Repository name:** `aqillah`
   - **Description:** `نظام وطني ذكي لتحليل الازدحام المروري - عَقِلْها`
   - اختر **Public** (أو Private إذا أردت)
   - ❌ **لا** تضع علامة على "Initialize this repository with a README"
   - ❌ **لا** تختار .gitignore أو license (لدينا بالفعل)
   
3. **اضغط "Create repository"**

---

### الخطوة 2: ربط المشروع المحلي بـ GitHub

بعد إنشاء المستودع، GitHub سيعرض لك الأوامر. استخدم هذه:

```powershell
# في Terminal (PowerShell) في مجلد المشروع C:\Aqqilha
git remote add origin https://github.com/YOUR_USERNAME/aqillah.git
git branch -M main
git push -u origin main
```

**استبدل `YOUR_USERNAME` باسمك على GitHub**

**مثال:**
```powershell
git remote add origin https://github.com/yakhawaji-lang/aqillah.git
git branch -M main
git push -u origin main
```

---

### الخطوة 3: إذا طُلب منك اسم المستخدم وكلمة المرور

**اسم المستخدم:** اسمك على GitHub

**كلمة المرور:** استخدم **Personal Access Token** (ليس كلمة المرور العادية)

#### كيفية إنشاء Personal Access Token:

1. اذهب إلى GitHub → Settings (من الصورة الشخصية في الأعلى)
2. في القائمة الجانبية، اضغط **"Developer settings"**
3. اضغط **"Personal access tokens"** → **"Tokens (classic)"**
4. اضغط **"Generate new token"** → **"Generate new token (classic)"**
5. املأ البيانات:
   - **Note:** `Vercel Deployment`
   - **Expiration:** اختر المدة (90 يوم أو سنة)
   - ✅ **repo** (Full control of private repositories)
6. اضغط **"Generate token"** في الأسفل
7. **انسخ الرمز** (سيظهر مرة واحدة فقط! لا تفقده)
8. استخدمه ككلمة مرور عند `git push`

---

### الخطوة 4: التحقق من الرفع

بعد `git push`، اذهب إلى:
```
https://github.com/YOUR_USERNAME/aqillah
```

ستجد جميع الملفات موجودة! ✅

---

## 🎯 الخطوات التالية بعد رفع المشروع على GitHub

بعد رفع المشروع على GitHub بنجاح:

1. **اذهب إلى Vercel:** https://vercel.com
2. **سجل بحساب GitHub**
3. **اضغط "New Project"**
4. **اختر المستودع `aqillah`**
5. **أضف Environment Variables**
6. **اضغط "Deploy"**

---

## ✅ قائمة التحقق

- [ ] تم إنشاء مستودع جديد على GitHub
- [ ] تم ربط المشروع المحلي بـ GitHub (`git remote add origin`)
- [ ] تم تغيير الفرع إلى `main` (`git branch -M main`)
- [ ] تم رفع المشروع (`git push -u origin main`)
- [ ] تم التحقق من وجود الملفات على GitHub

---

**بالتوفيق! 🚀**

