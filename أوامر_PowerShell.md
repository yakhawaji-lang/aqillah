# 💻 أوامر PowerShell لتشغيل البيانات - عَقِلْها

## 📋 الأوامر المباشرة

### 1️⃣ تشغيل البيانات الحية (30 دقيقة)

```powershell
npm run db:live
```

أو استخدم السكريبت الجاهز:
```powershell
.\تشغيل_البيانات.ps1
```

---

### 2️⃣ إنشاء بيانات تاريخية (سنة كاملة)

```powershell
npm run db:historical
```

أو استخدم السكريبت الجاهز:
```powershell
.\تشغيل_البيانات_التاريخية.ps1
```

> ⚠️ **تحذير**: هذا قد يستغرق وقتًا طويلاً (عدة دقائق)

---

### 3️⃣ إضافة بيانات تجريبية

```powershell
npm run db:seed
```

---

## 🚀 أوامر أخرى مفيدة

### تشغيل المشروع
```powershell
npm run dev
```

### إنشاء الجداول
```powershell
npm run db:generate
npm run db:push
```

### فتح Prisma Studio
```powershell
npm run db:studio
```

### بناء المشروع
```powershell
npm run build
npm start
```

---

## 📝 السكريبتات الجاهزة

تم إنشاء 3 سكريبتات PowerShell جاهزة للاستخدام:

### 1. `تشغيل_البيانات.ps1`
- يتحقق من المتطلبات تلقائيًا
- يشغل البيانات الحية لمدة 30 دقيقة
- يعرض رسائل واضحة

**الاستخدام**:
```powershell
.\تشغيل_البيانات.ps1
```

---

### 2. `تشغيل_البيانات_التاريخية.ps1`
- يطلب تأكيد قبل البدء
- ينشئ بيانات تاريخية لسنة كاملة
- يعرض تقدم العملية

**الاستخدام**:
```powershell
.\تشغيل_البيانات_التاريخية.ps1
```

---

### 3. `جميع_الأوامر.ps1`
- قائمة تفاعلية بجميع الأوامر
- سهولة في الاختيار والتنفيذ
- لا حاجة لحفظ الأوامر

**الاستخدام**:
```powershell
.\جميع_الأوامر.ps1
```

---

## 🎯 مثال كامل للتشغيل

```powershell
# 1. الانتقال لمجلد المشروع
cd C:\Aqqilha

# 2. تشغيل البيانات الحية (في نافذة منفصلة)
npm run db:live

# 3. في نافذة أخرى، تشغيل المشروع
npm run dev

# 4. فتح المتصفح
# http://localhost:3000
```

---

## ⚡ تشغيل سريع

### الطريقة 1: استخدام السكريبتات الجاهزة
```powershell
.\تشغيل_البيانات.ps1
```

### الطريقة 2: استخدام الأمر المباشر
```powershell
npm run db:live
```

### الطريقة 3: استخدام القائمة التفاعلية
```powershell
.\جميع_الأوامر.ps1
```

---

## 🔧 حل المشاكل

### إذا ظهر خطأ "لا يمكن تشغيل السكريبتات"

قم بتشغيل PowerShell كمسؤول وأدخل:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### إذا لم يعمل npm run db:live

تأكد من:
1. أن MySQL يعمل في XAMPP
2. أن قاعدة البيانات `aqillah` موجودة
3. أن الجداول تم إنشاؤها (`npm run db:push`)

---

## 📌 ملاحظات مهمة

1. **البيانات الحية**: تعمل لمدة 30 دقيقة ثم تتوقف تلقائيًا
2. **البيانات التاريخية**: قد تستغرق 5-10 دقائق حسب سرعة الجهاز
3. **لا تغلق النافذة**: اترك نافذة PowerShell مفتوحة أثناء تشغيل البيانات
4. **Ctrl+C**: اضغط لإيقاف السكريبت في أي وقت

---

## 🎮 أوامر سريعة (نسخ ولصق)

```powershell
# تشغيل البيانات الحية
npm run db:live

# إضافة بيانات تجريبية
npm run db:seed

# تشغيل المشروع
npm run dev

# إنشاء الجداول
npm run db:generate && npm run db:push
```

---

**تم التحديث**: 2024

