# 🔧 حل مشكلة عدم تحميل الخريطة

## 🔍 المشكلة
الخريطة لا تفتح أو لا تتحمل - تظهر رسالة "جاري تحميل الخريطة..." فقط

## ✅ الحلول

### 1. التحقق من المفتاح

افتح المتصفح واذهب إلى:
```
http://localhost:3000/api/test-maps
```

سترى:
- `hasApiKey`: يجب أن يكون `true`
- `apiKeyLength`: يجب أن يكون حوالي 39 حرف
- `apiKeyPrefix`: أول 10 أحرف من المفتاح

### 2. التحقق من ملف .env

تأكد من وجود المفاتيح في `.env`:
```env
NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY=AIzaSyDZgR_h8J5a4UsqmzRNFBlo28412mT25kQ
```

**مهم:** يجب أن يبدأ المتغير بـ `NEXT_PUBLIC_` للعمل في المتصفح!

### 3. إعادة تشغيل Dev Server

بعد إضافة المفتاح:
```bash
# أوقف السيرفر (Ctrl+C)
npm run dev
```

### 4. التحقق من Console

افتح Developer Tools (F12) واذهب إلى Console. ابحث عن:
- ✅ `Loading Google Maps with API key: AIzaSyDZ...`
- ✅ `Google Maps loaded successfully`
- ✅ `Map instance created`
- ✅ `Traffic layer enabled`
- ✅ `Map setup complete`

أو أخطاء مثل:
- ❌ `Google Maps API key not found`
- ❌ `Error loading Google Maps`
- ❌ `Invalid API key`

### 5. التحقق من قيود API Key

في Google Cloud Console:
1. اذهب إلى [API Credentials](https://console.cloud.google.com/apis/credentials)
2. افتح المفتاح
3. تأكد من:
   - ✅ **Maps JavaScript API** مفعل
   - ✅ **Routes API** مفعل
   - ✅ **Places API** مفعل
   - ✅ **Application restrictions**: HTTP referrers
   - ✅ **Website restrictions**: أضف `localhost:3000/*`

### 6. إضافة المفتاح يدوياً

إذا لم يعمل السكريبت:
```bash
npm run add-api-keys
```

أو أضف يدوياً في `.env`:
```env
NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY=AIzaSyDZgR_h8J5a4UsqmzRNFBlo28412mT25kQ
```

### 7. التحقق من Network Tab

في Developer Tools → Network:
- ابحث عن طلبات إلى `maps.googleapis.com`
- تحقق من Status Code:
  - ✅ `200` = نجح
  - ❌ `403` = مشكلة في المفتاح أو القيود
  - ❌ `400` = طلب غير صحيح

---

## 🐛 الأخطاء الشائعة

### خطأ: "Google Maps API key not configured"
**الحل:** أضف `NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY` في `.env`

### خطأ: "This API key is not authorized"
**الحل:** فعل APIs في Google Cloud Console:
- Maps JavaScript API
- Routes API
- Places API

### خطأ: "RefererNotAllowedMapError"
**الحل:** أضف `localhost:3000/*` في Website restrictions

### خطأ: "InvalidKeyMapError"
**الحل:** تحقق من أن المفتاح صحيح في `.env`

---

## ✅ بعد الإصلاح

1. أعد تشغيل dev server
2. افتح `http://localhost:3000/user`
3. يجب أن ترى الخريطة مع Traffic Layer

---

## 📞 إذا استمرت المشكلة

1. افتح Console (F12)
2. انسخ الأخطاء
3. تحقق من `/api/test-maps`
4. تأكد من أن المفتاح صحيح في Google Cloud Console

