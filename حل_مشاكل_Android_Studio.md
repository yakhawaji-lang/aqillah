# 🔧 حل مشاكل Android Studio Setup

## ⚠️ المشاكل الحالية

1. **مسار SDK يحتوي على مسافات**: `C:\Users\Yahya Khawaji\AppData\Local\Android\Sdk`
2. **مكونات مطلوبة مفقودة**: Android SDK غير متاح

---

## ✅ الحل السريع

### الخطوة 1: تغيير مسار SDK

في نافذة "SDK Components Setup":

1. **غير SDK Location** إلى:
   ```
   C:\Android\Sdk
   ```

2. **كيفية التغيير**:
   - اضغط على أيقونة المجلد 📁 بجانب المسار
   - أو اكتب مباشرة في الحقل: `C:\Android\Sdk`
   - اضغط **OK**

3. **تأكد من**:
   - المسار الجديد لا يحتوي على مسافات
   - المسار موجود أو سيتم إنشاؤه تلقائياً

---

### الخطوة 2: إنشاء المجلد (إذا لزم الأمر)

إذا لم يكن المجلد موجوداً، أنشئه:

```powershell
# في PowerShell (كمسؤول)
New-Item -ItemType Directory -Path "C:\Android\Sdk" -Force
```

أو يدوياً:
1. افتح File Explorer
2. اذهب إلى `C:\`
3. أنشئ مجلد جديد باسم `Android`
4. داخل `Android`، أنشئ مجلد باسم `Sdk`

---

### الخطوة 3: متابعة الإعداد

بعد تغيير المسار:

1. اضغط **OK** في نافذة التحذير
2. اضغط **Next** في نافذة Setup Wizard
3. انتظر تحميل SDK (10-20 دقيقة)
4. اضغط **Finish** عند الانتهاء

---

## 🔄 بعد اكتمال التحميل

### 1. تحديث متغيرات البيئة

```powershell
# تحديث ANDROID_HOME
[Environment]::SetEnvironmentVariable('ANDROID_HOME', 'C:\Android\Sdk', 'User')

# إعادة تشغيل PowerShell
```

أو استخدم السكريبت:

```powershell
npm run android:setup-env-user
```

### 2. التحقق

```powershell
npm run android:check
```

---

## 📋 خطوات مفصلة (بدون مسافات)

### في Android Studio:

1. **في نافذة "SDK Components Setup"**:
   - غير **Android SDK Location** من:
     ```
     C:\Users\Yahya Khawaji\AppData\Local\Android\Sdk
     ```
   - إلى:
     ```
     C:\Android\Sdk
     ```

2. **اضغط OK** في نافذة التحذير

3. **اضغط Next** للمتابعة

4. **انتظر** حتى يكتمل التحميل

5. **اضغط Finish**

---

## 🛠️ إنشاء المجلد تلقائياً

شغّل هذا الأمر في PowerShell (كمسؤول):

```powershell
# إنشاء المجلد
New-Item -ItemType Directory -Path "C:\Android\Sdk" -Force

# التحقق
Test-Path "C:\Android\Sdk"
```

---

## ✅ التحقق النهائي

بعد اكتمال كل شيء:

```powershell
# 1. التحقق من المتطلبات
npm run android:check

# 2. التحقق من ANDROID_HOME
echo $env:ANDROID_HOME
# يجب أن يكون: C:\Android\Sdk

# 3. التحقق من ADB
adb version
```

---

## 🎯 ملخص

1. ✅ غير مسار SDK إلى `C:\Android\Sdk`
2. ✅ اضغط OK في التحذيرات
3. ✅ اضغط Next وانتظر التحميل
4. ✅ بعد التحميل، حدث ANDROID_HOME
5. ✅ أعد تشغيل PowerShell
6. ✅ تحقق من التثبيت

---

**ملاحظة**: بعد تغيير المسار، سيبدأ Android Studio بتحميل SDK في الموقع الجديد.

