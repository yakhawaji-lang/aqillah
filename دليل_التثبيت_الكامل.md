# 📦 دليل التثبيت الكامل - متطلبات Android

## 🚀 الخطوات السريعة

### الطريقة التلقائية (موصى به)

```powershell
# 1. فتح صفحات التحميل
npm run android:download-java
npm run android:download-studio

# 2. بعد التثبيت، إعداد متغيرات البيئة
npm run android:setup-env

# 3. التحقق من التثبيت
npm run android:check
```

---

## 📋 الخطوات التفصيلية

### الخطوة 1: تثبيت Java JDK

#### أ) فتح صفحة التحميل

```powershell
npm run android:download-java
```

أو افتح يدوياً:
- **Eclipse Temurin (موصى به)**: https://adoptium.net/
- **Oracle JDK**: https://www.oracle.com/java/technologies/downloads/

#### ب) التحميل والتثبيت

**لـ Eclipse Temurin:**
1. اختر **JDK 17**
2. اختر **Windows x64**
3. اختر **.msi Installer**
4. اضغط **Download**
5. شغّل الملف المحمّل
6. اتبع معالج التثبيت (استخدم الإعدادات الافتراضية)

**لـ Oracle JDK:**
1. اختر **Java 17**
2. اختر **Windows x64 Installer**
3. اضغط **Download**
4. سجل دخول أو أنشئ حساب مجاني
5. شغّل الملف المحمّل
6. اتبع معالج التثبيت

#### ج) التحقق من التثبيت

```powershell
java -version
javac -version
```

يجب أن ترى إصدار Java 17 أو أحدث.

---

### الخطوة 2: تثبيت Android Studio

#### أ) فتح صفحة التحميل

```powershell
npm run android:download-studio
```

أو افتح يدوياً: https://developer.android.com/studio

#### ب) التحميل والتثبيت

1. اضغط **Download Android Studio**
2. اقرأ وقبل الشروط
3. اضغط **Download** مرة أخرى
4. شغّل الملف المحمّل (`android-studio-*.exe`)
5. اتبع معالج التثبيت:
   - اختر **Standard** installation
   - اضغط **Next** في جميع الخطوات
   - اضغط **Finish**

#### ج) الإعداد الأولي

عند فتح Android Studio لأول مرة:

1. **Setup Wizard**:
   - اختر **Standard** setup
   - اضغط **Next**

2. **SDK Components**:
   - سيتم تحميل Android SDK تلقائياً
   - انتظر حتى يكتمل التحميل (10-20 دقيقة)
   - اضغط **Finish**

3. **First Run**:
   - انتظر حتى يكتمل التحميل
   - اضغط **Finish**

#### د) التحقق من التثبيت

```powershell
adb version
```

يجب أن ترى إصدار ADB.

---

### الخطوة 3: إعداد متغيرات البيئة

#### الطريقة التلقائية (موصى به)

```powershell
# شغّل PowerShell كمسؤول (Run as Administrator)
npm run android:setup-env
```

السكريبت سيقوم بـ:
- البحث عن Java JDK تلقائياً
- البحث عن Android SDK تلقائياً
- إضافة JAVA_HOME و ANDROID_HOME
- إضافة المسارات إلى PATH

#### الطريقة اليدوية

1. افتح **System Properties**:
   - اضغط `Win + R`
   - اكتب `sysdm.cpl`
   - اضغط Enter

2. **Environment Variables**:
   - اضغط **Environment Variables**
   - في **System variables**، اضغط **New**

3. أضف **JAVA_HOME**:
   - **Variable name**: `JAVA_HOME`
   - **Variable value**: `C:\Program Files\Java\jdk-17` (أو مسار التثبيت الفعلي)
   - اضغط **OK**

4. أضف **ANDROID_HOME**:
   - **Variable name**: `ANDROID_HOME`
   - **Variable value**: `C:\Users\YourUsername\AppData\Local\Android\Sdk`
   - (استبدل YourUsername باسم المستخدم الخاص بك)
   - اضغط **OK**

5. أضف إلى **Path**:
   - اختر **Path** → **Edit**
   - اضغط **New** وأضف:
     - `%JAVA_HOME%\bin`
     - `%ANDROID_HOME%\platform-tools`
     - `%ANDROID_HOME%\tools`
     - `%ANDROID_HOME%\tools\bin`
   - اضغط **OK** في جميع النوافذ

6. **أعد تشغيل PowerShell** لتطبيق التغييرات

---

### الخطوة 4: التحقق من التثبيت

```powershell
# فحص سريع
npm run android:check

# فحص تفصيلي
npm run android:install
```

يجب أن ترى:
- ✓ Node.js مثبت
- ✓ npm مثبت
- ✓ Java JDK مثبت
- ✓ Android SDK موجود
- ✓ Android Studio مثبت

---

## 🔧 حل المشاكل

### المشكلة: "java is not recognized"

**الحل**:
1. تأكد من إضافة `%JAVA_HOME%\bin` إلى Path
2. أعد تشغيل PowerShell
3. تحقق: `java -version`

### المشكلة: "adb is not recognized"

**الحل**:
1. تأكد من إضافة `%ANDROID_HOME%\platform-tools` إلى Path
2. أعد تشغيل PowerShell
3. تحقق: `adb version`

### المشكلة: "JAVA_HOME is not set"

**الحل**:
1. شغّل: `npm run android:setup-env`
2. أو أضف JAVA_HOME يدوياً (راجع الخطوة 3)

### المشكلة: Android SDK لا يتم تحميله

**الحل**:
1. افتح Android Studio
2. **Tools** → **SDK Manager**
3. تأكد من تثبيت:
   - Android SDK Platform-Tools
   - Android SDK Build-Tools
   - Android SDK Platform (API 33)
4. اضغط **Apply** وانتظر

---

## ✅ التحقق النهائي

بعد إكمال جميع الخطوات:

```powershell
# 1. فحص المتطلبات
npm run android:check

# 2. إذا كانت جميع المتطلبات موجودة:
npm run android:setup

# 3. بناء المشروع
npm run android:build
```

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع قسم "حل المشاكل" أعلاه
2. راجع `android-requirements.md`
3. تأكد من إعادة تشغيل PowerShell بعد تعديل متغيرات البيئة

---

**تم التحديث**: 2024

