# دليل رفع موقع عَقِلْها على الاستضافة

## 📋 المتطلبات الأساسية

### المتطلبات التقنية:
- ✅ Node.js 18+ 
- ✅ MySQL 8.0+ أو MariaDB
- ✅ npm أو yarn
- ✅ Git

### متغيرات البيئة المطلوبة:

**قاعدة البيانات:**
```
DATABASE_URL="mysql://username:password@host:3306/database_name"
```

**Google Maps APIs:**
```
NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY=your_google_maps_api_key
AQILLAH_MAPS_WEB_KEY=your_google_maps_api_key
AQILLAH_ROUTES_KEY=your_routes_api_key
AQILLAH_PLACES_KEY=your_places_api_key
```

**OpenWeatherMap API:**
```
OPENWEATHER_API_KEY=your_openweather_api_key
```

**إعدادات التطبيق:**
```
NEXT_PUBLIC_APP_URL=https://your-domain.com
NODE_ENV=production
```

---

## 🌐 خيارات الاستضافة الموصى بها

### 1. Vercel (الأسهل والأسرع) ⭐ موصى به

**المميزات:**
- ✅ دعم ممتاز لـ Next.js (من صناع Next.js)
- ✅ إعداد تلقائي
- ✅ SSL مجاني
- ✅ تحديثات تلقائية من Git
- ✅ خطة مجانية متاحة

**الخطوات:**

1. **إنشاء حساب على Vercel:**
   - اذهب إلى https://vercel.com
   - سجل بحساب GitHub/GitLab/Bitbucket

2. **ربط المشروع:**
   - اضغط "New Project"
   - اختر المستودع من GitHub
   - Vercel سيكتشف Next.js تلقائياً

3. **إعداد متغيرات البيئة:**
   - في إعدادات المشروع → Environment Variables
   - أضف جميع المتغيرات المطلوبة

4. **إعداد قاعدة البيانات:**
   - استخدم Vercel Postgres (مدفوع) أو
   - استخدم قاعدة بيانات خارجية (PlanetScale, Railway, أو أي MySQL host)

5. **Deploy:**
   - اضغط "Deploy"
   - انتظر حتى يكتمل البناء
   - الموقع سيكون متاحاً على `your-project.vercel.app`

**ملاحظة:** Vercel لا يدعم MySQL مباشرة، يمكنك استخدام:
- **PlanetScale** (MySQL متوافق)
- **Railway** (يدعم MySQL)
- **أي MySQL host خارجي**

---

### 2. Railway

**المميزات:**
- ✅ دعم MySQL مباشرة
- ✅ إعداد سهل
- ✅ خطة مجانية متاحة
- ✅ SSL مجاني

**الخطوات:**

1. **إنشاء حساب:**
   - اذهب إلى https://railway.app
   - سجل بحساب GitHub

2. **إنشاء مشروع جديد:**
   - اضغط "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المستودع

3. **إضافة MySQL Database:**
   - اضغط "New" → "Database" → "MySQL"
   - Railway سينشئ قاعدة بيانات تلقائياً

4. **إعداد متغيرات البيئة:**
   - في إعدادات المشروع → Variables
   - أضف جميع المتغيرات المطلوبة
   - `DATABASE_URL` سيتم إنشاؤه تلقائياً من MySQL service

5. **Deploy:**
   - Railway سيبني المشروع تلقائياً
   - بعد البناء، سيتم نشر الموقع

---

### 3. Render

**المميزات:**
- ✅ دعم MySQL
- ✅ خطة مجانية متاحة
- ✅ SSL مجاني

**الخطوات:**

1. **إنشاء حساب:**
   - اذهب إلى https://render.com
   - سجل بحساب GitHub

2. **إنشاء Web Service:**
   - اضغط "New" → "Web Service"
   - اختر المستودع
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`

3. **إضافة MySQL Database:**
   - اضغط "New" → "PostgreSQL" أو "MySQL"
   - انسخ `DATABASE_URL` من إعدادات Database

4. **إعداد متغيرات البيئة:**
   - في إعدادات Web Service → Environment
   - أضف جميع المتغيرات المطلوبة

5. **Deploy:**
   - اضغط "Create Web Service"
   - انتظر حتى يكتمل البناء

---

### 4. DigitalOcean App Platform

**المميزات:**
- ✅ دعم MySQL
- ✅ أداء عالي
- ✅ خطة مدفوعة (من $5/شهر)

**الخطوات:**

1. **إنشاء حساب:**
   - اذهب إلى https://www.digitalocean.com
   - سجل حساب جديد

2. **إنشاء App:**
   - اضغط "Create" → "App"
   - اختر المستودع من GitHub

3. **إضافة Database:**
   - اضغط "Add Resource" → "Database"
   - اختر MySQL
   - اختر الخطة المناسبة

4. **إعداد متغيرات البيئة:**
   - في إعدادات App → Environment Variables
   - أضف جميع المتغيرات

5. **Deploy:**
   - اضغط "Create Resources"
   - انتظر حتى يكتمل البناء

---

## 📝 خطوات الإعداد العامة

### الخطوة 1: إعداد قاعدة البيانات

#### أ) إنشاء قاعدة البيانات:

```sql
CREATE DATABASE aqillah CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### ب) إنشاء مستخدم (اختياري):

```sql
CREATE USER 'aqillah_user'@'%' IDENTIFIED BY 'strong_password';
GRANT ALL PRIVILEGES ON aqillah.* TO 'aqillah_user'@'%';
FLUSH PRIVILEGES;
```

#### ج) الحصول على DATABASE_URL:

```
mysql://username:password@host:3306/aqillah
```

---

### الخطوة 2: إعداد متغيرات البيئة

أنشئ ملف `.env.production` في المشروع:

```env
# قاعدة البيانات
DATABASE_URL="mysql://username:password@host:3306/aqillah"

# Google Maps APIs
NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY=your_google_maps_api_key
AQILLAH_MAPS_WEB_KEY=your_google_maps_api_key
AQILLAH_ROUTES_KEY=your_routes_api_key
AQILLAH_PLACES_KEY=your_places_api_key

# OpenWeatherMap API
OPENWEATHER_API_KEY=your_openweather_api_key

# إعدادات التطبيق
NEXT_PUBLIC_APP_URL=https://your-domain.com
NODE_ENV=production
```

**⚠️ مهم:** لا ترفع ملف `.env` إلى Git! تأكد من إضافته إلى `.gitignore`

---

### الخطوة 3: تحديث إعدادات Google Cloud Console

1. **اذهب إلى Google Cloud Console**
2. **افتح مشروعك**
3. **API Credentials → Edit API Key**
4. **أضف Domain الاستضافة إلى "Application restrictions":**
   - `your-domain.com`
   - `*.your-domain.com`
   - `your-project.vercel.app` (إذا كنت تستخدم Vercel)

---

### الخطوة 4: بناء المشروع محلياً (اختياري للاختبار)

```bash
# تثبيت الحزم
npm install

# توليد Prisma Client
npm run db:generate

# بناء المشروع
npm run build

# اختبار محلي
npm start
```

---

### الخطوة 5: رفع المشروع

#### الطريقة الأولى: رفع عبر Git (موصى به)

1. **أنشئ مستودع Git:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **ارفع إلى GitHub/GitLab:**
   ```bash
   git remote add origin https://github.com/your-username/your-repo.git
   git push -u origin main
   ```

3. **اربط المستودع بالاستضافة:**
   - في إعدادات الاستضافة، اختر "Deploy from Git"
   - اختر المستودع
   - الاستضافة ستبني وتنشر تلقائياً

#### الطريقة الثانية: رفع مباشر (FTP/SFTP)

1. **بناء المشروع:**
   ```bash
   npm run build
   ```

2. **رفع الملفات:**
   - ارفع مجلد `.next` و `public` و `node_modules`
   - ارفع `package.json` و `next.config.js`
   - ارفع جميع ملفات المشروع

3. **تشغيل على الخادم:**
   ```bash
   npm start
   ```

---

## 🔧 إعدادات خاصة بكل استضافة

### Vercel

**ملف `vercel.json` (اختياري):**

```json
{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "DATABASE_URL": "@database_url",
    "NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY": "@maps_key"
  }
}
```

**ملاحظات:**
- Vercel لا يدعم MySQL مباشرة
- استخدم PlanetScale أو Railway للـ MySQL
- أو استخدم Vercel Postgres (يتطلب تعديل Prisma schema)

---

### Railway

**ملف `railway.json` (اختياري):**

```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

---

### Render

**ملف `render.yaml` (اختياري):**

```yaml
services:
  - type: web
    name: aqillah
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NEXT_PUBLIC_AQILLAH_MAPS_WEB_KEY
        sync: false
```

---

## 🗄️ إعداد قاعدة البيانات على الاستضافة

### بعد رفع المشروع:

1. **الاتصال بقاعدة البيانات:**
   ```bash
   # على الخادم
   npx prisma db push
   ```

2. **توليد Prisma Client:**
   ```bash
   npm run db:generate
   ```

3. **إضافة بيانات تجريبية (اختياري):**
   ```bash
   npm run db:seed
   ```

---

## ✅ قائمة التحقق قبل الرفع

- [ ] تحديث `DATABASE_URL` بمعلومات قاعدة البيانات الجديدة
- [ ] تحديث `NEXT_PUBLIC_APP_URL` بعنوان الموقع الجديد
- [ ] إضافة Domain إلى Google Cloud Console API restrictions
- [ ] التأكد من أن جميع API keys صحيحة
- [ ] إضافة `.env` إلى `.gitignore`
- [ ] اختبار البناء محلياً (`npm run build`)
- [ ] التأكد من عمل قاعدة البيانات
- [ ] رفع المشروع إلى Git (إذا كان استخدام Git)
- [ ] إعداد متغيرات البيئة على الاستضافة
- [ ] Deploy واختبار الموقع

---

## 🚀 بعد الرفع

### التحقق من عمل الموقع:

1. **افتح الموقع:**
   ```
   https://your-domain.com
   ```

2. **اختبر الصفحات:**
   - `/` - الصفحة الرئيسية
   - `/map` - الخريطة
   - `/dashboard` - لوحة التحكم
   - `/user` - واجهة المستخدم

3. **اختبر APIs:**
   ```
   https://your-domain.com/api/traffic
   https://your-domain.com/api/alerts
   ```

### إصلاح المشاكل الشائعة:

**مشكلة: خطأ في قاعدة البيانات**
- تحقق من `DATABASE_URL`
- تأكد من أن قاعدة البيانات موجودة
- تحقق من صلاحيات المستخدم

**مشكلة: Google Maps لا يعمل**
- تحقق من API key
- تأكد من إضافة Domain إلى restrictions
- تحقق من تفعيل APIs المطلوبة

**مشكلة: الصفحات لا تعمل**
- تحقق من `NEXT_PUBLIC_APP_URL`
- تأكد من بناء المشروع بشكل صحيح
- تحقق من logs الاستضافة

---

## 📊 خيارات قاعدة البيانات السحابية

### 1. PlanetScale (موصى به لـ MySQL)

**المميزات:**
- ✅ MySQL متوافق 100%
- ✅ خطة مجانية متاحة
- ✅ سريع وموثوق

**الخطوات:**
1. اذهب إلى https://planetscale.com
2. أنشئ حساب جديد
3. أنشئ Database جديد
4. انسخ `DATABASE_URL` من إعدادات Database

---

### 2. Railway MySQL

**المميزات:**
- ✅ سهل الإعداد
- ✅ متكامل مع Railway
- ✅ خطة مجانية

---

### 3. DigitalOcean Managed Database

**المميزات:**
- ✅ أداء عالي
- ✅ نسخ احتياطي تلقائي
- ✅ خطة مدفوعة (من $15/شهر)

---

## 💡 نصائح مهمة

1. **استخدم Environment Variables:**
   - لا تضع API keys في الكود
   - استخدم متغيرات البيئة دائماً

2. **اختبار محلي أولاً:**
   - اختبر البناء محلياً قبل الرفع
   - تأكد من عمل قاعدة البيانات

3. **مراقبة Logs:**
   - راقب logs الاستضافة
   - تحقق من الأخطاء بانتظام

4. **النسخ الاحتياطي:**
   - احتفظ بنسخة احتياطية من قاعدة البيانات
   - استخدم Git للنسخ الاحتياطي للكود

5. **الأمان:**
   - استخدم HTTPS دائماً
   - لا تشارك API keys
   - استخدم كلمات مرور قوية

---

## 🎯 الخلاصة السريعة

**للاستضافة السريعة (موصى به):**

1. **Vercel** + **PlanetScale** (MySQL)
   - أسهل إعداد
   - خطة مجانية متاحة
   - أداء ممتاز

2. **Railway** (كل شيء في مكان واحد)
   - MySQL + Next.js في نفس المكان
   - إعداد سهل
   - خطة مجانية متاحة

**الخطوات الأساسية:**
1. أنشئ حساب على الاستضافة
2. اربط المشروع من Git
3. أضف قاعدة بيانات MySQL
4. أضف متغيرات البيئة
5. Deploy!

---

**بالتوفيق! 🚀**

