# حل مشكلة Weather Layer

## المشكلة
النقر على الخريطة أو على marker لعرض بيانات الطقس لا يعمل.

## الأسباب المحتملة

1. **Weather API غير متاح**: قد لا يكون API key موجوداً أو غير صحيح
2. **Click listener غير مضاف بشكل صحيح**: قد يكون listener غير مضاف عند تفعيل الطبقة
3. **مشكلة في استيراد WeatherInfoWindow**: قد يكون هناك مشكلة في الاستيراد

## الحلول المطبقة

### 1. إصلاح استيراد WeatherInfoWindow
- ✅ إضافة import مباشر في أعلى الملف
- ✅ إزالة dynamic import

### 2. إصلاح click listener
- ✅ استخدام `useRef` للـ weatherInfoWindow بدلاً من state
- ✅ إضافة click listener بشكل ديناميكي عند تفعيل `showWeatherLayer`
- ✅ تنظيف listener عند إلغاء التفعيل

### 3. إضافة معالجة أخطاء أفضل
- ✅ إضافة console.log للتحقق من الطلبات
- ✅ عرض رسائل خطأ واضحة للمستخدم
- ✅ إضافة mock data عند فشل API (للاختبار)

## التحقق من الحل

### 1. افتح Console (F12)
راقب الرسائل:
- `🌤️ Fetching weather for:` - يعني أن الطلب بدأ
- `🌤️ Weather data received:` - يعني أن البيانات وصلت
- `✅ Weather info window opened` - يعني أن النافذة فُتحت
- `❌ Error fetching weather:` - يعني أن هناك خطأ

### 2. تحقق من Weather API
افتح في المتصفح:
```
http://localhost:3000/api/weather/point?lat=24.7136&lng=46.6753
```

إذا كان هناك خطأ 500:
- تأكد من إضافة `OPENWEATHER_API_KEY` في `.env`
- أو استخدم mock data (يتم إرجاعها تلقائياً عند فشل API)

### 3. تحقق من تفعيل الطبقة
- تأكد من تفعيل checkbox "🌤️ عرض بيانات الطقس"
- يجب أن تظهر weather markers على الخريطة

## خطوات الاختبار

1. افتح صفحة `/map`
2. فعّل checkbox "🌤️ عرض بيانات الطقس"
3. انتظر حتى تظهر weather markers
4. انقر على الخريطة في أي مكان
5. يجب أن تظهر InfoWindow ببيانات الطقس

## إذا استمرت المشكلة

1. **تحقق من Console**: ابحث عن أخطاء JavaScript
2. **تحقق من Network**: ابحث عن طلبات `/api/weather/point` في Network tab
3. **تحقق من API Key**: تأكد من وجود `OPENWEATHER_API_KEY` في `.env`
4. **جرب Mock Data**: إذا لم يكن API key موجوداً، سيتم استخدام mock data تلقائياً

