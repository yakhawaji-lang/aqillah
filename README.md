# عَقِلْها (Aqillah) - نظام وطني ذكي لتحليل الازدحام المروري

نظام وطني ذكي داخل منظومة أبشر، يعتمد على بيانات تحركات الجوال المجهولة لتحليل الازدحام المروري وتقديم تنبؤات وتوجيهات ذكية.

## المميزات الرئيسية

- 📊 **تحليل الازدحام المروري في الوقت الفعلي**: مراقبة مستمرة لحالة الطرق
- 🔮 **التنبؤ بالازدحام قبل وقوعه**: نماذج ذكية للتنبؤ بالازدحام المستقبلي
- 🗺️ **خرائط تفاعلية**: عرض مرئي لحالة الازدحام على الخرائط
- 📱 **تطبيق متجاوب**: يعمل على جميع الأجهزة (هواتف، أجهزة لوحية، حواسيب)
- 🎯 **لوحة تحكم حكومية**: نظرة شاملة للمسؤولين والجهات الحكومية
- 🔔 **نظام تنبيهات ذكي**: تنبيهات فورية مع اقتراحات مسارات بديلة
- 🔒 **حماية الخصوصية**: بيانات مجهولة تمامًا بدون أي هوية شخصية
- 🔗 **تكامل مع أبشر**: متكامل مع المنظومة الحكومية أبشر

## التقنيات المستخدمة

### Frontend
- **Next.js 14** مع App Router
- **React 18** مع TypeScript
- **Tailwind CSS** للتصميم
- **Leaflet** للخرائط التفاعلية
- **Recharts** للرسوم البيانية
- **React Query** لإدارة البيانات

### Backend
- **Next.js API Routes** (Serverless Functions)
- **MySQL** قاعدة البيانات (XAMPP)
- **Prisma ORM** لإدارة قاعدة البيانات
- **Zod** للتحقق من البيانات

### الأدوات
- **TypeScript** للأنواع الآمنة
- **ESLint** للجودة البرمجية
- **Git** للتحكم بالإصدارات

## متطلبات التشغيل

- Node.js 18+ 
- XAMPP (MySQL)
- npm أو yarn

## التثبيت والتشغيل

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd Aqqilha
```

### 2. تثبيت الحزم

```bash
npm install
```

### 3. إعداد قاعدة البيانات

#### أ) تشغيل MySQL في XAMPP
1. افتح XAMPP Control Panel
2. اضغط Start بجانب MySQL

#### ب) إنشاء قاعدة البيانات
1. افتح phpMyAdmin: `http://localhost/phpmyadmin`
2. أنشئ قاعدة بيانات جديدة باسم `aqillah`
3. اختر Collation: `utf8mb4_unicode_ci`

#### ج) إعداد ملف البيئة
أنشئ ملف `.env` في الجذر:

```env
DATABASE_URL="mysql://root:@localhost:3306/aqillah"
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

> 📝 إذا كان لديك كلمة مرور لـ MySQL، أضفها: `mysql://root:password@localhost:3306/aqillah`

#### د) إنشاء الجداول
```bash
# توليد Prisma Client
npm run db:generate

# إنشاء الجداول في قاعدة البيانات
npm run db:push

# (اختياري) إضافة بيانات تجريبية
npm run db:seed
```

> 📖 **للتفاصيل الكاملة**: راجع ملف `خطوات_التشغيل.md`

### 4. تشغيل المشروع

```bash
# وضع التطوير
npm run dev

# بناء للإنتاج
npm run build
npm start
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

## هيكل المشروع

```
├── app/                    # صفحات Next.js (App Router)
│   ├── api/               # API Routes
│   ├── dashboard/         # لوحة التحكم الرئيسية
│   ├── map/               # صفحة الخريطة
│   ├── alerts/            # صفحة التنبيهات
│   └── government/        # لوحة التحكم الحكومية
├── components/            # مكونات React قابلة لإعادة الاستخدام
│   ├── Header.tsx         # رأس الصفحة
│   ├── TrafficMap.tsx     # مكون الخريطة
│   ├── CongestionIndicator.tsx
│   └── ...
├── lib/                   # مكتبات ومساعدات
│   ├── prisma.ts          # Prisma Client
│   ├── utils.ts           # دوال مساعدة
│   └── hooks/             # React Hooks مخصصة
├── prisma/                # مخطط قاعدة البيانات
│   └── schema.prisma      # مخطط Prisma
├── public/                # الملفات الثابتة
├── types/                 # تعريفات TypeScript
└── scripts/               # سكريبتات مساعدة
```

## الصفحات الرئيسية

### 1. لوحة التحكم (`/dashboard`)
- نظرة شاملة على حالة المرور
- إحصائيات مباشرة
- خريطة تفاعلية
- قائمة التنبيهات النشطة

### 2. الخريطة (`/map`)
- خريطة تفاعلية كاملة الشاشة
- فلترة حسب المدينة
- بحث عن الطرق
- تفاصيل كل مقطع طريق

### 3. التنبيهات (`/alerts`)
- جميع التنبيهات النشطة
- فلترة حسب النوع والخطورة
- اقتراحات مسارات بديلة

### 4. لوحة الحكومة (`/government`)
- لوحة تحكم متقدمة للمسؤولين
- رسوم بيانية وتحليلات
- تنبؤات مستقبلية
- إحصائيات شاملة

## API Endpoints

### `/api/traffic`
جلب بيانات المرور الحالية
- `GET /api/traffic?city=الرياض`

### `/api/predictions`
جلب التنبؤات المستقبلية
- `GET /api/predictions?segmentId=xxx&minutesAhead=20`

### `/api/alerts`
جلب التنبيهات النشطة
- `GET /api/alerts?city=الرياض&activeOnly=true`

### `/api/stats`
جلب الإحصائيات العامة
- `GET /api/stats`

## التكامل مع أبشر

النظام مصمم للتكامل مع منظومة أبشر الحكومية:

1. **API Integration**: واجهات برمجية للتكامل
2. **Push Notifications**: إشعارات للمستخدمين
3. **SSO**: تسجيل دخول موحد (يمكن إضافته)

راجع `components/AbesherIntegration.tsx` للتفاصيل.

## الأمان والخصوصية

- ✅ **بيانات مجهولة**: لا يتم تخزين أي هوية شخصية
- ✅ **تجميع البيانات**: البيانات تأتي مجمعة من مزودي الخدمة
- ✅ **التحقق**: Zod للتحقق من صحة البيانات
- ✅ **HTTPS**: استخدام HTTPS في الإنتاج

## التطوير

### إضافة بيانات تجريبية

```bash
npm run db:seed
```

أو عبر API:

```bash
POST /api/seed
```

### فتح Prisma Studio

```bash
npm run db:studio
```

### فحص الأخطاء

```bash
npm run lint
```

## النشر

### Vercel (موصى به)

1. اربط المستودع مع Vercel
2. أضف متغيرات البيئة
3. Vercel سيقوم بالبناء والنشر تلقائياً

### Docker (قريباً)

سيتم إضافة ملفات Docker قريباً.

## المساهمة

هذا مشروع حكومي. للمساهمة، يرجى التواصل مع الفريق المسؤول.

## الترخيص

مشروع حكومي - جميع الحقوق محفوظة © 2024

## الدعم

للدعم الفني، يرجى التواصل عبر:
- البريد الإلكتروني: support@aqillah.sa
- الهاتف: 1999

---

**عَقِلْها** - نظام وطني ذكي لتحليل الازدحام المروري 🚗📊

