# 🎯 الإعداد النهائي الكامل - تطبيق Android

## ✅ ما تم إنجازه حتى الآن

- ✓ Node.js مثبت (v22.20.0)
- ✓ npm مثبت (10.9.3)
- ✓ Java JDK 17 مثبت
- ✓ Android Studio مثبت
- ✓ Command Line Tools مثبت
- ✓ متغيرات البيئة مضبوطة

---

## 🔧 الخطوات النهائية المتبقية

### الخطوة 1: تثبيت platform-tools (ADB)

افتح PowerShell وانتقل إلى:

```powershell
cd C:\Android\Sdk\cmdline-tools\latest\bin
```

ثم شغّل:

```powershell
# تعيين متغيرات البيئة
$env:ANDROID_HOME = "C:\Android\Sdk"
$env:ANDROID_SDK_ROOT = "C:\Android\Sdk"

# قبول التراخيص (اكتب y لكل ترخيص)
.\sdkmanager.bat --licenses

# تثبيت platform-tools
.\sdkmanager.bat "platform-tools" --sdk_root=C:\Android\Sdk

# تثبيت Platform 33
.\sdkmanager.bat "platforms;android-33" --sdk_root=C:\Android\Sdk

# تثبيت Build Tools
.\sdkmanager.bat "build-tools;33.0.0" --sdk_root=C:\Android\Sdk
```

---

### الخطوة 2: تحديث PATH في الجلسة الحالية

```powershell
$env:Path += ";C:\Android\Sdk\platform-tools;C:\Android\Sdk\tools;C:\Android\Sdk\cmdline-tools\latest\bin"
```

---

### الخطوة 3: التحقق

```powershell
adb version
```

يجب أن ترى إصدار ADB.

---

### الخطوة 4: تحديث متغيرات البيئة بشكل دائم

```powershell
# تحديث ANDROID_HOME
[Environment]::SetEnvironmentVariable('ANDROID_HOME', 'C:\Android\Sdk', 'User')
[Environment]::SetEnvironmentVariable('ANDROID_SDK_ROOT', 'C:\Android\Sdk', 'User')

# تحديث PATH
$userPath = [Environment]::GetEnvironmentVariable("Path", "User")
$pathsToAdd = @(
    "C:\Android\Sdk\platform-tools",
    "C:\Android\Sdk\tools",
    "C:\Android\Sdk\cmdline-tools\latest\bin"
)

foreach ($path in $pathsToAdd) {
    if ($userPath -notlike "*$path*") {
        $userPath = if ($userPath) { "$userPath;$path" } else { $path }
    }
}

[Environment]::SetEnvironmentVariable("Path", $userPath, "User")
```

---

### الخطوة 5: إعادة تشغيل PowerShell

**مهم**: أغلق PowerShell وافتح واحدة جديدة.

---

### الخطوة 6: التحقق النهائي

```powershell
# التحقق من adb
adb version

# التحقق من جميع المتطلبات
npm run android:check
```

يجب أن ترى:
- ✓ Node.js
- ✓ npm
- ✓ Java
- ✓ Android SDK

---

### الخطوة 7: إعداد Capacitor

```powershell
# تثبيت Capacitor (إذا لم يكن مثبتاً)
npm install @capacitor/core @capacitor/cli @capacitor/android --save-dev

# تهيئة Capacitor
npx cap init

# عند الطلب:
# App name: عَقِلْها
# App ID: sa.gov.aqillah
# Web dir: out

# إضافة منصة Android
npx cap add android

# مزامنة
npx cap sync android
```

---

### الخطوة 8: بناء المشروع

```powershell
# بناء المشروع لـ Android
npm run build:android

# أو
npm run build
# ثم
npx cap sync android
```

---

### الخطوة 9: فتح في Android Studio

```powershell
npx cap open android
```

---

### الخطوة 10: بناء APK

في Android Studio:
1. **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. انتظر حتى يكتمل البناء
3. اضغط **locate** لفتح مجلد APK

---

## 📋 الأوامر السريعة (نسخ ولصق)

### تثبيت SDK Components

```powershell
cd C:\Android\Sdk\cmdline-tools\latest\bin
$env:ANDROID_HOME = "C:\Android\Sdk"
$env:ANDROID_SDK_ROOT = "C:\Android\Sdk"
.\sdkmanager.bat --licenses
.\sdkmanager.bat "platform-tools" --sdk_root=C:\Android\Sdk
.\sdkmanager.bat "platforms;android-33" --sdk_root=C:\Android\Sdk
.\sdkmanager.bat "build-tools;33.0.0" --sdk_root=C:\Android\Sdk
```

### تحديث PATH

```powershell
$env:Path += ";C:\Android\Sdk\platform-tools;C:\Android\Sdk\tools;C:\Android\Sdk\cmdline-tools\latest\bin"
adb version
```

### تحديث متغيرات البيئة

```powershell
[Environment]::SetEnvironmentVariable('ANDROID_HOME', 'C:\Android\Sdk', 'User')
[Environment]::SetEnvironmentVariable('ANDROID_SDK_ROOT', 'C:\Android\Sdk', 'User')
$userPath = [Environment]::GetEnvironmentVariable("Path", "User")
$userPath += ";C:\Android\Sdk\platform-tools;C:\Android\Sdk\tools;C:\Android\Sdk\cmdline-tools\latest\bin"
[Environment]::SetEnvironmentVariable("Path", $userPath, "User")
```

---

## 🎯 الخلاصة

1. ✅ تثبيت platform-tools يدوياً
2. ✅ تحديث PATH
3. ✅ إعادة تشغيل PowerShell
4. ✅ التحقق
5. ✅ إعداد Capacitor
6. ✅ بناء المشروع
7. ✅ فتح في Android Studio
8. ✅ بناء APK

---

**تم التحديث**: 2024

