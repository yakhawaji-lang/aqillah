# ⚡ بدء سريع - عَقِلْها

## الخطوات السريعة (5 دقائق)

### 1️⃣ تشغيل MySQL
- افتح **XAMPP** → اضغط **Start** بجانب **MySQL**

### 2️⃣ فتح PowerShell في مجلد المشروع
```powershell
cd C:\Aqqilha
```

### 3️⃣ تثبيت المكتبات (للمرة الأولى فقط)
```powershell
npm install
```

### 4️⃣ إنشاء قاعدة البيانات
- افتح `http://localhost/phpmyadmin`
- اضغط **New** → اسم: `aqillah` → **Create**

### 5️⃣ إعداد ملف .env
أنشئ ملف `.env` في مجلد المشروع:
```env
DATABASE_URL="mysql://root:@localhost:3306/aqillah"
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 6️⃣ إنشاء الجداول
```powershell
npm run db:generate
npm run db:push
```

### 7️⃣ إضافة بيانات تجريبية
```powershell
npm run db:seed
```

### 8️⃣ تشغيل المشروع
```powershell
npm run dev
```

### 9️⃣ فتح المتصفح
```
http://localhost:3000
```

---

## 🎯 الصفحات المتاحة

- **الصفحة الرئيسية**: `http://localhost:3000`
- **تطبيق المستخدم**: `http://localhost:3000/user`
- **لوحة الحكومة**: `http://localhost:3000/government`

---

## 📖 للتفاصيل الكاملة
راجع ملف `خطوات_التشغيل.md`

